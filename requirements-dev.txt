-r requirements.txt

# Test-only dependencies. Not needed to run the API, only to test it.
pytest==9.1.1
httpx==0.28.1       # required by fastapi.testclient.TestClient
fakeredis==2.37.1   # in-memory Redis stand-in so tests don't need a live Redis server
